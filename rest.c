/*Eric Smithson
10/5/2013
Calculates rest stops*/
#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main(void) {
    int trip_length;
    printf("How long is your road trip, in miles?\n");
    //For whatever reason trip_length never returns what I set it to.
    scanf("%d", &trip_length);
    printf("%d", &trip_length);
    /*printf("What is the distance between stops for gas, in miles?\n");
    printf("What is the distance between stops for food, in miles?\n");
    return 0; */
}
